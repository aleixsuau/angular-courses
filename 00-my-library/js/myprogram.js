var userName = "Aleix";
externalLibrary.saludarComplex(userName);

function saludar () {
    // console.log("Hola");
    // saludarComplex(userName);
    externalLibrary.saludarComplex(userName);
}

function aplaudir () {
    // console.log("Plas, plas, plas");
    // aplaudirComplex(userName);
    externalLibrary.aplaudirComplex(userName);
}

/*function printHola () {
    externalLibrary.print("Hola " + userName);
}*/